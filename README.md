# node-prometheus-example
A very simple example on how to create an endpoint for Prometheus default metrics.

## How to run

Run `node index.js` and open your browser and go to `http://localhost:8080/metrics`.
